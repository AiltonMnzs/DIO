sudo apt install php php-mbstring php-gd php-xml php-bcmath php-ldap php-mysql
sudo apt install build-essential libpcre3-dev libssl-dev libevent-dev libmariadb-dev libsnmp-dev libopenipmi-dev libssh2-1-dev libxml2-dev libcurl4-gnutls-dev libldap2-dev libiksemel-dev libgnutls28-dev fping
sudo apt install pkg-config
sudo apt install apache2



*sudo apt install mariadb-server
*sudo mysql_secure_installation

mkdir /downloadszabbix
dentro da pasta :
wget https://cdn.zabbix.com/zabbix/sources/stable/6.4/zabbix-6.4.0.tar.gz
tar -zxvf zabbix-6.4.0.tar.gz
cd zabbix-6.4.0


sudo addgroup --system --quiet zabbix
sudo adduser --quiet --system --disabled-login --ingroup zabbix --home /var/lib/zabbix --no-create-home zabbix

export CFLAGS="-std=gnu99"
./configure --enable-server --enable-agent --with-mysql --with-openssl --enable-ipv6 --with-net-snmp --with-libcurl --with-libxml2

make install

sudo mysql -u root -p
CREATE DATABASE zabbix character set utf8 collate utf8_bin;
CREATE USER 'zabbix'@'localhost' IDENTIFIED BY '123321oi';
GRANT ALL PRIVILEGES ON zabbix.* TO 'zabbix'@'localhost';
set global log_bin_trust_function_creators = 1;
FLUSH PRIVILEGES;
EXIT;

mysql -uzabbix -p zabbix < database/mysql/schema.sql
mysql -uzabbix -p zabbix < database/mysql/images.sql
mysql -uzabbix -p zabbix < database/mysql/data.sql


# mysql -uroot -p
password
mysql> set global log_bin_trust_function_creators = 0;
mysql> quit;
